#prime
#all x in [2,n), n%x!=0 (n>1)

def find(n):
    x=2
    while x<n:
        if n%x==0:
            return x
        x=x+1
    return x

n= input('integer >> ')


if n>1 and not(find(n)<n):
    print 'prime'
else:
    print 'not prime'
